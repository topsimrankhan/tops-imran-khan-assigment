//area of triangle
#include <stdio.h> 
      

int main() 
{
	int area;
	int base = 7;
	int height = 5;
          area = height * base;
	printf("Area of the triangle = %d square inches\n", area);

return(0);
}
