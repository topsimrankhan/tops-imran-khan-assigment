//Area of the Circle
#include <stdio.h> 
int main()
 {
   int radius;
   float area;    
   radius = 6;

          area = 3.14*radius*radius;
	printf("Area of the Circle = %f square inches\n", area);

return(0);
}
